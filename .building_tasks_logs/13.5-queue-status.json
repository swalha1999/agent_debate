{
  "task_id": "13.5",
  "title": "get_queue_status — complete depth+stats snapshot, surfaced to logs/UI — Epic 13",
  "date": "2026-05-31",
  "model": "claude-opus-4-8",
  "prompt": "Implement get_queue_status() returning queue depth and stats; surface to logs and the UI.\n\nFollow repo standards: TDD (write the failing test first, then code); max 150 lines per code file (split, don't compress); ruff = 0 violations + mypy clean; no hard-coded values (read from config); every external API call goes through the API gatekeeper (Epic 13); log via the LOG package; keep coverage >= 85%. Read docs/PRD.md and the relevant docs/prds/*.md before starting.",
  "tokens": {
    "input": null,
    "output": null,
    "total": null
  },
  "usage": {
    "tool_uses": null,
    "duration_ms": null,
    "summary": "usage to be filled by main loop from harness report"
  },
  "note": "Fifth task of Epic 13 (docs/prds/api-gatekeeper.md §3/§6: get_queue_status reports depth + stats). Makes the status snapshot COMPLETE, accurate, and observable. TDD red-first: wrote packages/core/tests/test_gatekeeper_status.py (7 tests) and watched them fail (ImportError: no GatekeeperStatus), then implemented to green. types.py: extended per-service QueueStatus with backpressure_total + in_flight fields (frozen Pydantic, ge=0); added GatekeeperStatus aggregate model (services: dict[str, QueueStatus] + total_depth/enqueued/drained/backpressure/in_flight) with a from_services() classmethod that sums per-service counters — JSON-serializable via model_dump_json so Epic 11 UI/API/SSE can read it unchanged. Real state, no hard-coded values: depth/enqueued/drained/max_depth from _OverflowQueue, new backpressure_total counter (_queue.record_backpressure(), wired in _enqueue on a full-queue rejection), in_flight read via new _RateLimiter.in_flight() (reusing the 13.2/13.4 _inflight seam). _gatekeeper: get_queue_status() now returns the full per-service QueueStatus; added get_status() (per-service + aggregate across every live queue) and log_queue_status() (snapshot + emit one 'system' event tagged queue_event='status' carrying the flattened snapshot, then return it — on-demand so it never spams). New _events._GatekeeperLog.status() emitter flattens GatekeeperStatus.model_dump() into the LOG payload. Exported GatekeeperStatus from gatekeeper/__init__. Existing gatekeeper tests stayed green (additive field/method changes; old QueueStatus assertions still hold). Gates green locally: uv sync clean; ruff check . = All checks passed; ruff format --check . clean; mypy = no issues in 63 source files; pytest --cov = 237 passed, 100% total coverage (>=85 required); check_line_limit.py rc=0 (all files <=150 code lines); secret_scan.py rc=0. docs/PROMPTS.md entry added."
}
