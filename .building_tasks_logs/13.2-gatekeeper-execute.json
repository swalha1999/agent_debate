{
  "task_id": "13.2",
  "title": "ApiGatekeeper.execute (check rate limits, run, log service/latency/outcome) — Epic 13",
  "date": "2026-05-31",
  "model": "claude-opus-4-8",
  "prompt": "Implement ApiGatekeeper.execute(api_call, *args, **kwargs): check rate limits before running, execute, and log every call (service, latency, outcome).\n\nFollow repo standards: TDD (write the failing test first, then code); max 150 lines per code file (split, don't compress); ruff = 0 violations + mypy clean; no hard-coded values (read from config); every external API call goes through the API gatekeeper (Epic 13); log via the LOG package; keep coverage >= 85%. Read docs/PRD.md and the relevant docs/prds/*.md before starting.",
  "tokens": {
    "input": null,
    "output": null,
    "total": null
  },
  "usage": {
    "summary": "usage to be filled by main loop from harness report"
  },
  "note": "Second task of Epic 13 (the API gatekeeper, docs/prds/api-gatekeeper.md): the CORE ApiGatekeeper class with execute(). Builds on 13.1's config surface (RateLimitConfig/ServiceLimits/load_rate_limit_config in the gatekeeper subpackage). Scope kept tight per the sub-PRD: a simple in-process limit CHECK + execute + LOG now; FIFO overflow queue (13.3), retry/concurrency (13.4), full get_queue_status (13.5) hook in at marked seams later. TDD red-first: wrote packages/core/tests/test_gatekeeper_execute.py first and watched it fail (ImportError: cannot import name 'ApiGatekeeper'), then implemented. Split across the gatekeeper subpackage to stay well under 150 code lines and give later tasks clean homes: _gatekeeper.py (ApiGatekeeper class, 67 code lines), _limiter.py (per-service sliding-window _RateLimiter with injectable clock, 35), types.py (QueueStatus, CallOutcome, 18), errors.py (RateLimitExceededError — Error suffix for ruff N818, 7). execute(api_call, *args, service='default', **kwargs): picks the service's ServiceLimits via config.get_service_limits (default fallback), calls _check_limits BEFORE running (an exhausted requests_per_minute/requests_per_hour window raises RateLimitExceededError — this is the seam 13.3 swaps for FIFO enqueue/backpressure), times the call with time.monotonic, executes, returns result. Logs EXACTLY ONE structured event per call via the LOG package (log_event, event_type='tool_call', agent='gatekeeper', payload={service, outcome}, latency_ms, round=0) on BOTH success (outcome='success') and error (outcome='error', then re-raise — retry is 13.4). Gatekeeper takes run_id + runs_dir for logging context: ApiGatekeeper(config, run_id=..., runs_dir=...). _RateLimiter: per-service deque of monotonic timestamps; check() prunes hour-old entries then compares per-minute (count within 60s) and per-hour (total within 3600s) against config ceilings, returning the exhausted window name or None; record() appends a timestamp on admit; a concurrent-count seam (_inflight) is in place for 13.4. 0 hard-coded limits: every threshold comes from the injected RateLimitConfig; the only literals are unit conversions (60s/3600s/1000ms) and identifier labels (agent='gatekeeper', event_type='tool_call', window names matching config keys). Re-exported ApiGatekeeper + QueueStatus + RateLimitExceededError from agent_debate.core.gatekeeper and agent_debate.core. Added packages/core/tests/test_gatekeeper_limiter.py (3 tests, fake injectable clock) to deterministically cover both windows + pruning without real sleeps. docs/PROMPTS.md entry added. Gates green locally: uv sync clean; ruff check . = All checks passed; ruff format --check . = 56 files already formatted; mypy = no issues in 56 source files; pytest --cov = 213 passed, 100% total coverage (>=85 required); check_line_limit.py rc=0 (largest new file 67 code lines, well under 150); secret_scan.py rc=0."
}
